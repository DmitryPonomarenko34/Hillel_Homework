// Engine
// import { setLoading, setItems } from '../slice';

function* callGetTodosWorker() {
  try {
  } catch (e) {
    console.warn(e);
  }
}

function* callAddTodosWorker(action) {
  try {
  } catch (e) {
    console.warn(e);
  }
}

export { callGetTodosWorker, callAddTodosWorker };
